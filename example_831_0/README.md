# Example 831_0

This folder is the **public lightweight example package** for:

- `scan = JeFG25nYj2p`
- `instr_id = 831_0`

It keeps only the final preview assets that are most useful for browsing the project without shipping the full local rendering history.

## Files

- `latest_bev_topo.png`
  - Final recommended static `topo + BEV` overlay.

- `latest_graph_nav_frame.png`
  - Final recommended single frame in the familiar graph-nav layout.

- `latest_bev_trajectory.gif`
- `latest_bev_trajectory.mp4`
  - Final recommended animated top-down trajectory result.

- `agent_fpv_trajectory.gif`
- `agent_fpv_trajectory.mp4`
  - First-person RGB trajectory previews.

- `agent_fpv_contact_sheet.png`
  - Compact contact sheet summarizing the first-person frames.

## Notes

- The full local experiment history for `831_0` is larger and includes many intermediate rendering variants.
- Those heavy assets are intentionally excluded from the public branch to keep the repository pushable and easier to clone.
